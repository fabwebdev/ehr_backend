// View configuration that matches Laravel's config/view.php
const viewConfig = {
  // View Storage Paths
  paths: [
    'resources/views',
  ],
  
  // Compiled View Path
  compiled: 'storage/framework/views',
};

export default viewConfig;